<Alloy>
	<TableViewRow onClick="clickRow">
		<Label id="name" text="{name}"></Label>
		<Label id="uuid" text="{uuid}"></Label>

		<View id="rssi">
			<Label text="{rssi}"></Label>
			<Label >db</Label>
		</View>
	</TableViewRow>
</Alloy>
